FROM osrf/ros:humble-desktop

# Install Python packages for VLM
RUN apt-get update && apt-get install -y \
    python3-pip \
    git \
    && rm -rf /var/lib/apt/lists/*

# Remove conflicting system packages
RUN apt-get update && apt-get remove -y python3-sympy && apt-get autoremove -y && rm -rf /var/lib/apt/lists/*

# Upgrade pip
RUN pip3 install --upgrade pip

# Install PyTorch with CUDA support
RUN pip3 install --no-cache-dir torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Install latest transformers from GitHub (has Qwen2.5-VL support)
RUN pip3 install --no-cache-dir git+https://github.com/huggingface/transformers.git

# Install other VLM packages
RUN pip3 install --no-cache-dir \
    accelerate peft bitsandbytes \
    pillow pymavlink opencv-python \
    qwen-vl-utils

WORKDIR /app

# Source ROS2 setup in bashrc
RUN echo "source /opt/ros/humble/setup.bash" >> ~/.bashrc
